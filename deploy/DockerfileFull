FROM python:3.8


RUN apt-get update && apt-get install -y --no-install-recommends pandoc libreoffice texlive-latex-base

ADD ./requirements.txt .requirements.txt
RUN pip3 install -r .requirements.txt


RUN pip3 install pytest

ADD . .
RUN pip install -e .




# Run the tests
CMD ["pytest", "tests"]
